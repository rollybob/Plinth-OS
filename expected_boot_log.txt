# Smoke test contract: every non-blank, non-comment line below must appear
# in the captured serial output, in order. See xtask check_smoke_output.

plinth: kernel entry
plinth: frame allocator ready
plinth: GDT + TSS loaded
plinth: IDT loaded
plinth: syscall interface ready

# ACPI MADT discovery (broader hardware, Stage A1): parse the RSDP -> XSDT ->
# MADT for the CPU + interrupt-controller topology. Pure discovery -- the 8259
# PIC still drives interrupts; this only reports what an APIC bring-up (Stage A2)
# will consume. Only the stable count summary is asserted: the per-entry detail
# lines (LAPIC base, I/O APIC base/GSI, source overrides) carry addresses that
# can shift across QEMU versions, so they are skipped like the PCI BAR lines.
# Counts are stable under the -smp 1 q35 smoke config: 1 CPU, 1 I/O APIC.
plinth: acpi: 1 cpu(s), 1 ioapic(s)

plinth: timer armed

# Console input bring-up (Stage 1): the i8042 keyboard comes up as the first
# event source and IRQ1 is unmasked. Scancodes flow through input::record, which
# routes them to any ring subscribed to the source (event_rings.md); the evt/kbd
# demos below prove that producer -> subscription -> reader path end to end, so
# there is no separate boot selftest line.
plinth: keyboard ready (i8042, IRQ1)

# Mouse bring-up (Design/mouse_input.md): the i8042's second port comes up as
# the second event source (source 1) and IRQ12 is unmasked. QEMU's q35 machine
# always exposes a PS/2 mouse on this port, so this line is deterministic under
# the smoke config; a real-hardware port without one would instead see "no
# mouse detected" and skip the mouse demo below.
plinth: mouse ready (i8042 port 2, IRQ12)

# Storage bring-up: discover virtio-blk over legacy PCI config space and bring
# up every device. Two disks are attached: device 0 (slot 3) is the 1 MiB
# byte-ramp test image, device 1 (slot 4) is the read-only boot archive. Each
# is proved with a single polled read of sector 0 -- the ramp disk against the
# deterministic ramp, the archive disk only as a distinct, readable second
# device (the kernel does not parse the archive format; that is the FS libOS's
# job). Only stable summary lines are asserted: the per-BAR / per-capability
# lines carry addresses that can shift across QEMU versions, and device 1's
# capacity line is omitted because the archive size tracks the user ELFs.
plinth: scanning PCI bus
plinth: virtio-blk found at 00:03.0
plinth: virtio-blk found at 00:04.0
plinth: virtio-blk[0] ready (queue 0, size 64, capacity 2048 sectors)

# MSI-X completions (broader hardware, Stage A3, D7): once Stage A2's LAPIC is
# up, each device's block-completion interrupt moves from a shared INTx line to
# its own MSI-X table entry, retiring the ISR-status deassert read Stage 4
# needed. Printed (and asserted) right after each device's bring-up, before
# the polled sector-0 reads below. The assigned vectors are deterministic
# (MSIX_VECTOR_BASE + device index, no addresses), so a silent regression back
# to INTx would change or drop these lines.
plinth: virtio-blk[0] msix vector 0x30
plinth: virtio-blk[1] msix vector 0x31
plinth: virtio-blk[0] sector 0 read ok (ramp verified)
plinth: virtio-blk[1] sector 0 read ok (distinct disk)

plinth: running hello
hello: ring 3
hello: frame mapped and writable
hello: done
plinth: hello exited (code 0)

# Same workload, bump policy: c cannot fit in the frame's remaining
# 1024 bytes, so the libOS asks the kernel for a second frame.
plinth: running bump-demo
demo: policy = bump
demo: a = 0x10000000
demo: b = 0x10000600
demo: freed a
demo: c = 0x10000c00
demo: c got a new address
demo: kernel frames used: 2
plinth: bump-demo exited (code 0)

# Deliberate null dereference: the kernel logs the ring-3 #PF, kills the
# process, and the boot continues.
plinth: running crash-demo
crash: about to dereference null
plinth: [user fault] #PF
plinth: terminating user process
plinth: crash-demo faulted

# Same workload, free-list policy: c reuses a's freed block -- same
# address back, still only one kernel frame.
plinth: running list-demo
demo: policy = freelist
demo: a = 0x10000000
demo: b = 0x10000600
demo: freed a
demo: c = 0x10000000
demo: c reused a freed block
demo: kernel frames used: 1
plinth: list-demo exited (code 0)

# CPU time as a capability: greedy-demo is minted a fixed CPU budget at
# spawn and spends it in a loop until the kernel cuts it off -- the
# resource-bound twin of the fault demo. Same teardown path, no leak.
plinth: running greedy-demo
greedy: spending CPU budget
greedy: charged 256, remaining = 768
greedy: charged 256, remaining = 512
greedy: charged 256, remaining = 256
greedy: charged 256, remaining = 0
plinth: [out of budget] terminating user process
plinth: greedy-demo out of budget

# Self-paging: lazy-demo registers a ring-3 fault handler, then touches
# unmapped pages. Each first touch faults; the kernel upcalls the handler,
# which maps a frame and resumes the instruction. Same #PF as crash-demo,
# opposite outcome -- the policy lives in userspace.
plinth: running lazy-demo
lazy: registering fault handler
lazy: serviced fault at 0x18000000
lazy: serviced fault at 0x18001000
lazy: serviced fault at 0x18002000
lazy: serviced fault at 0x18003000
lazy: all pages materialized on demand
plinth: lazy-demo exited (code 0)

# Phase 2: the preemptive scheduler launches independent CPU-bound processes
# and round-robins them under the timer. Their spin[...] lines interleave --
# that ordering is checked per-process by xtask (check_per_process_order), not
# here, since the cross-process interleaving is deliberately nondeterministic.
# These two lines bracket the demo; the frame counts around them must match
# (check_frames_baseline), proving no process leaked.
plinth: scheduler demo: 3 processes
plinth: scheduler demo: all done

# Phase 2 IPC: a pinger and a ponger rendezvous over one synchronous endpoint
# the kernel grants to both. Their "ping N got .." / "pong N got .." lines
# interleave -- checked per-process (and for correct exchanged values) by
# xtask, not here. The frame counts around the demo must match (no leak).
plinth: ipc demo: 2 processes
plinth: ipc demo: all done

# Phase 2 IPC, capability transfer: a producer fills a frame and hands its
# capability to a consumer over IPC; the consumer maps the same physical frame
# and reads the data back. The producer/consumer lines (and the read-back
# value) are checked per-process by xtask, not here. The frame counts around
# the demo must match -- ownership moved, so the frame is reclaimed once.
plinth: share demo: 2 processes
plinth: share demo: all done

# Phase 2 IPC, call/reply (RPC): a client calls a server over one endpoint
# (client SEND-only, server RECV-only). The server answers each call via a
# one-shot reply capability. The client's "call N got .." lines and the
# returned values are checked by xtask; frame counts must match.
plinth: rpc demo: 2 processes
plinth: rpc demo: all done

# Phase 2 IPC, spawn reconciliation: the kernel launches one parent; the parent
# spawns a worker as an independent scheduled process, the worker sends its
# result over the channel spawn set up, and the parent collects it with recv
# (the join). The returned value is checked by xtask; frame counts must match.
plinth: spawn demo: 1 processes
plinth: spawn demo: all done

# Phase 2 storage, BlockRange multiplexing: the kernel grants a process a
# BlockRange capability naming disk sectors [1,5); the process reads sector 1
# through it (bytes verified against the image ramp: byte j == (1+j)&0xFF) and
# is denied a read one sector past its range. The frame counts around the demo
# must match (check_frames_baseline) -- the I/O frame is reclaimed at teardown.
plinth: blk demo: 1 processes
blk: read ok b0=1 b1=2 b5=6
blk: out-of-range rejected
plinth: blk demo: all done

# Async block demo (Stage 3): depth made observable. asyncblk issues four reads
# that overlap on the device through the libos reference futures-executor over
# the completion rings, then asserts each landed in its own frame. The "ok" line
# is the only deterministic output -- completion ORDER is the device's to choose,
# so correctness is asserted inside the demo, never transcript-matched. Frame
# counts around it must match (its I/O + ring frames are reclaimed at teardown).
plinth: asyncblk demo: 1 processes
asyncblk: ok (4 reads overlapped, each frame verified)
plinth: asyncblk demo: all done

# Block write path (Design/block_write.md): the write half of the ring ABI.
# The kernel grants blkwrite-user a BlockRange over device 0 sectors [8,12),
# minted with RIGHT_WRITE instead of RIGHT_READ -- the multiplexing guarantee
# now gates the write direction. The demo writes a fixed pattern, reads the
# same range back into a separate frame, and asserts the bytes match the
# pattern (not the disk's original ramp content), proving the write reached
# the device. Frame counts around it must match (check_frames_baseline).
plinth: blkwrite demo: 1 processes
blkwrite: ok (write+read-back verified)
plinth: blkwrite demo: all done

# Phase 2 storage, load-from-disk: the FS library-OS demo. The kernel grants
# fsdemo a BlockRange over the archive device; fsdemo uses libfs to parse the
# on-disk archive, find "diskhello" by name, read its ELF off the disk, and
# launch it via spawn_from_buffer. diskhello is NOT embedded in the kernel (it
# lives only in the archive), so its "running from disk" line proves the load
# path end to end; it reports 777 back over the spawn result channel. The frame
# counts around the demo must match (check_frames_baseline) -- fsdemo's and
# diskhello's frames are all reclaimed at teardown.
plinth: fs demo: 1 processes
fsdemo: loading 'diskhello' from the boot archive
diskhello: running from disk
fsdemo: diskhello returned 777
plinth: fs demo: all done

# Phase 2 console input (Stage 2): the kernel grants evt-user a read capability
# on input source 0 (the keyboard). The process is rejected reading through a
# non-source capability, then reads one event through its EventSource. The ring
# is empty so it blocks; the kernel idles on input and a synthetic scancode
# (0x1e, 'A' make) is delivered to wake it deterministically (a real keypress
# would otherwise). The scancode is raw -- turning it into a character is libOS
# policy. Frame counts around the demo must match (check_frames_baseline).
plinth: evt demo: 1 processes
evt: non-source rejected
evt: got scancode 0x1e
plinth: evt demo: all done

# Phase 2 console input (Stage 3): a multishot event STREAM. The kernel grants
# evtstream-user the same keyboard EventSource; the process opens one multishot
# subscription through the libos async executor (a stream future over the
# completion-ring reactor) and reaps a SEQUENCE of events -- one RING_OP_EVENT_SUB,
# many completions demuxed by the subscription cookie. A scripted scancode
# sequence ('a','b','c','d' make codes) is injected to drive it; the demo asserts
# each event arrived once and in order (not transcript-matched), then cancels the
# subscription. Frame counts around the demo must match (check_frames_baseline).
plinth: evtstream demo: 1 processes
evtstream: start
evtstream: ok (4 events in order)
plinth: evtstream demo: all done

# Phase 2 console input (Stage 3): a line read through a library OS. kbd-user
# uses libinput (an unprivileged keymap + line reader) to turn raw scancodes
# into a line and echo it -- "input is output-only" retired, the keymap as libOS
# policy. The scripted scancodes spell "Hi" (shift down, h, shift up, i, enter),
# so the keymap's shift handling shows up in the echoed line. Frame counts
# around the demo must match (check_frames_baseline).
plinth: kbd demo: 1 processes
kbd: type a line
kbd: read 'Hi'
plinth: kbd demo: all done

# Phase 2 console input (Stage 4): the unified loop -- ONE ring multiplexes block
# I/O and input. The kernel grants unified-user a BlockRange over device 0 (slot
# 1) and the keyboard EventSource (slot 2); the process registers a single ring,
# issues a block read AND a multishot keyboard subscription on it, and drives both
# to completion in one block_on/ring_wait loop (join2). The disk completion
# arrives via the virtio IRQ; a scripted scancode sequence ('x','y','z' make
# codes) drives the input side. The demo asserts the read landed sector 0's ramp
# bytes and each event arrived in order, then cancels. Frame counts around the
# demo must match (check_frames_baseline).
plinth: unified demo: 1 processes
unified: start
unified: ok (1 read + 3 events on one ring)
plinth: unified demo: all done

# Mouse input (Design/mouse_input.md S2): a second EventSource. mouse-user
# subscribes to source 1 (the mouse) through the same libos stream adapter
# evtstream-user uses, and reaps a scripted 3-packet sequence (dx, dy, buttons
# per packet), asserting each decodes correctly and in order, then cancels.
# Frame counts around the demo must match (check_frames_baseline).
plinth: mouse demo: 1 processes
mouse: start
mouse: ok (3 packets in order)
plinth: mouse demo: all done

plinth: boot ok
