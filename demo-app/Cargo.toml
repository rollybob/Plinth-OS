# Standalone crate -- intentionally not a workspace member.
[workspace]

[package]
name = "demo-app"
version = "0.1.0"
edition = "2021"

[dependencies]
libplinth = { path = "../libplinth" }
libos = { path = "../libos" }
